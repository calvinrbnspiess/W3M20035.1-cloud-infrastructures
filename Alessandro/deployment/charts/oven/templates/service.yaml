apiVersion: v1
kind: Service
metadata:
  name: {{ include "oven.fullname" . }}
  labels:
    {{- include "oven.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oven.selectorLabels" . | nindent 4 }}
