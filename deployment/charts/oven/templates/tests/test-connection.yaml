apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oven.fullname" . }}-test-connection"
  labels:
    {{- include "oven.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oven.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
