apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ofen.fullname" . }}-test-connection"
  labels:
    {{- include "ofen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ofen.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
